name: idony_mobile
description: A native mobile client for the Idony AI system.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_markdown: ^0.6.18
  provider: ^6.0.5
  image_picker: ^1.0.4
  file_picker: ^6.1.1
  shared_preferences: ^2.2.2
  intl: ^0.18.1
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
